package cmd

import (
/*	"bytes"
	"text/template"*/
)

func Template(source string) string {
	/*	var buf bytes.Buffer
		tmpl, err := template.New("test").Parse(source)
		err = tmpl.Execute(&buf,)*/

}
